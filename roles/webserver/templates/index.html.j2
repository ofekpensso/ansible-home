<!DOCTYPE html>
<html>
<head>
    <title>{{ my_title }}</title>
    <style>
        body { font-family: sans-serif; text-align: center; padding: 50px; background-color: #f0f0f0; }
        .box { background: white; padding: 20px; border-radius: 10px; display: inline-block; }
        ul { text-align: left; }
    </style>
</head>
<body>
    <div class="box">
        <h1>Welcome to {{ ansible_hostname }} 🚀</h1>
        <p>This page was built automatically by Ansible.</p>
        
        <h3>Installed Packages:</h3>
        <ul>
            {% for pkg in server_packages %}
                <li>✅ {{ pkg }}</li>
            {% endfor %}
            </ul>

        <p>Generated at: {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
    </div>
</body>
</html>
